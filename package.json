{
  "dependencies": {
    "express": "^4.18.2"
  },
  "name": "web_static",
  "description": "Deploy Static html to heroku",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  }
}
